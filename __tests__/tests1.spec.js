describe("Filter function", () => {
    test("it should get that 2 equals 2)", () => {
        expect(2).toEqual(2);
    });
    test("it should get that true equals true)", () => {
        expect(true).toEqual(false);
    });
  });